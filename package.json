{
  "name": "capex-wbs-control-hub-vrt",
  "private": true,
  "version": "0.1.0",
  "description": "Visual regression baselines for the approved CAPEX & WBS Control Hub UI. styles.css is the client-approved visual baseline and must not change; these tests prove it.",
  "scripts": {
    "vrt": "playwright test",
    "vrt:update": "playwright test --update-snapshots",
    "vrt:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
